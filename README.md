# hr_attendance_system
The HR Attendance Management System is a backend application developed using FastAPI and SQLite. It enables organizations to manage employee attendance, leave requests, and reporting efficiently.  The system provides APIs for employee registration and login, attendance tracking (check-in/check-out),
